export interface IApiCurrency {
  id: string;
  code: string;
  name: string;
  shortName: string;
  symbol: string;
}

export interface ICurrency {
  id: string;
  code: string;
  name: string;
  shortName: string;
  symbol: string;
}
