extends ../../layout

block content
  p
    | #{t('transactionalEmails.resetPassword.greeting')}
    |
  p
    | #{t('transactionalEmails.resetPassword.instruction')}
    |

    a(href=locals.url || "#") #{locals.url}

  br
  p
    | | #{t('transactionalEmails.resetPassword.ignore')}
    br
