extends ../../layout

block content
  p
    | #{t('transactionalEmails.greeting', {name: locals.name})}
    |
  p
    | #{t('transactionalEmails.signUpConfirmation.message')}
    | #{t('transactionalEmails.signUpConfirmation.instruction')}
    |
    br
    a(href=locals.url || "#") #{locals.url}
  br
  p
    | #{t('transactionalEmails.signUpConfirmation.ignore')}
    br
